# 残課題タスク

最終更新日: 2026-02-18

## Zedシンタックスハイライト（後日実装）

### T-001: `tree-sitter-dokusy` の作成
- 状態: 未着手
- 優先度: 高
- 目的: `dokusy` 専用grammarで正確な構文解析とハイライトを実現する。
- 完了条件:
  - `tree-sitter-dokusy` リポジトリまたは同梱grammarが作成済み
  - Zed拡張側で grammar を `dokusy` に切り替え可能

### T-002: `highlights.scm` の拡張
- 状態: 未着手
- 優先度: 中
- 目的: 現在の最小ハイライトに、キーワード/演算子等の色分けを段階的に追加する。
- 完了条件:
  - `let`, `mut`, `fn`, `if`, `else`, `while`, `return` が適切にハイライトされる
  - `==`, `!=`, `<`, `<=`, `>`, `>=`, `&&`, `||` を含む演算子がハイライトされる
  - Zedログに query error が出ない

### T-003: 補助クエリの追加（任意）
- 状態: 未着手
- 優先度: 低
- 目的: 編集体験を改善する。
- 完了条件:
  - `outline.scm` を追加
  - 必要に応じて `indents.scm` / `brackets.scm` を追加

